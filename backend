# Use the official Node.js image as the base image
FROM node:18-alpine

# Set the working directory inside the container
WORKDIR /app/backend

# Copy package.json and package-lock.json (or yarn.lock if using yarn)
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy the rest of the application code
COPY . .

# If your backend needs to be compiled (e.g., TypeScript to JavaScript)
# RUN npm run build # Uncomment if you have a build step for your backend

# Expose the port your backend service will listen on
EXPOSE 8080

# Command to run the application
CMD [ "npm", "start" ]
